SESSION:
- session.params
- sid (length - 250)
- anti-hijacking
- input-field
- flash
- old
- csrf
- encrypt 
- token cookie
(on top of aura session)[DONE!]

DI:
- try league/container (REJECTED!: no lazing loading)

dev:
- debugbar (DONE!)

- tactician (DONE!)

* Convert functions to classes (PARTIAL)

* redirect response (DONE!)
config (DONE!)
Cache (DONE!)

flysystem
router resource map 

write interfaces for each component

monolog 
swiftmailer

